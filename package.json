{
  "name": "repo-root",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --filter @gustavolmo/react-window-manager --filter frontend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}